name: Daily AI Newsroom

on:
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - name: Create article
        run: |
          echo "<html><body><h1>Daily Edition</h1><p>This is your AI newsroom working.</p></body></html>" > ai-edition.html

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          title: "Daily AI Edition"
          body: "Review today's AI-generated articles before publishing."
          commit-message: "New daily edition"
          branch: daily-edition
          base: main
          add-paths: |
            ai-edition.html
